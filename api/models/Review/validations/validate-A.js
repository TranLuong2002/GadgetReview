/**
* Validation code for field userName on Review
* @param {import("gadget-server").ReviewUserNameFieldValidationContext } context - All the useful bits for running this validation.
*/
export default async ({api, record, errors, logger, field}) => {
  // TODO: implement validation
  // access things like `record.userName` to get the fields's data
  // add to the `errors` with `errors.add('userName', "did not pass the validation")`
};